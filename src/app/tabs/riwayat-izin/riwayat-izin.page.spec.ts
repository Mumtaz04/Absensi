import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RiwayatIzinPage } from './riwayat-izin.page';

describe('RiwayatIzinPage', () => {
  let component: RiwayatIzinPage;
  let fixture: ComponentFixture<RiwayatIzinPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RiwayatIzinPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
